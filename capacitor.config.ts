import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tugas10_H1D021053',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
